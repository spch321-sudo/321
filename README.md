# 落實321 — App（v1.0.2）

晨讀321 全套一百七十三課的陪讀 PWA（繁體／簡體）。以《321建造 教會領導力》App v1.3.16 的殼層為基底，承襲其語音朗讀（Azure 真人語音＋跟讀自動捲動）、句子層級八色畫線與領受、小智 AI 陪讀、提詞機、四級字級、深淺色等全部功能；並針對「落實」新增每課「讀／享／講／帶」四模式、操練勾選、信心宣告、920操練簿、十單元分包載入與資料備份。無框架、無建置步驟，全部檔案放在 repo 根目錄即可部署到 GitHub Pages。

## 檔案（全部放在同一層，不用子資料夾）

```
index.html            App 殼層＋全部 CSS（沿用領導力 App；右上角 繁／简 切換）
app.js                全部邏輯：路由、四模式、朗讀、畫線、陪讀、提詞機、工具、我的
sw.js                 Service Worker：版本化快取＋資料分包 stale-while-revalidate
manifest.json         PWA 安裝設定
icon-72 … icon-1024.png  十個尺寸圖示（主視覺：十字架與金色道路）
data.zh.index.json    十單元索引（課名、副題、字數、關鍵詞計數）— 開機只載這一個（63 KB）
data.zh.u1 … u10.json 十個單元分包（每課含完整版、部落格、12/21張投影片文字稿、精簡版、逐字稿、
                      主題經文、操練、信心宣告、回應禱告、小組討論、經文索引）— 依需要載入並背景預抓
data.zh.tracks.json   關鍵詞軌跡（14 個關鍵詞在 173 課的出現位置）— 開「關鍵詞軌跡」時才載
data.zs.*.json        以上全部的簡體版（OpenCC 轉換），切到「简」時載入
```

## 五個分頁

1. **今日** — 繼續閱讀、**讀經計畫**（每日一課約半年／每週一課約三年半／看聽想講行速成 33 課，每天告訴你該讀哪一課）、**每日提醒**（本機通知，App 開著或從背景回來時到點提醒；可設時間）、**複習宣告**（完成後第 3、7、30 天把該課的信心宣告再提出來）、今日金句、十單元環形進度。
2. **課程** — 十單元 173 課手風琴（自動展開你上次所在的單元），每單元附主題經文。每課四模式：
   - **讀**：完整版講章（主題經文、引言、經文背景、要點、321整合、應用操練、結語），句子畫線＋領受＋問小智、真人語音朗讀跟讀、操練勾選（記入操練簿、可指定為哪一位「2」操練）、信心宣告（點一下宣告句存入宣告簿，或自己寫）、回應禱告、小組討論、標記完成、上一課／下一課。
   - **享**：部落格文章，一鍵複製全文／系統分享（含標籤與課別署名）。
   - **講**：12 張（15 分鐘）／21 張（30 分鐘）投影片文字，每張可展開「講解重點」與「口語逐字稿」；一鍵匯出投影片文字貼進簡報軟體；直接進入提詞機。
   - **帶**：全螢幕提詞機（投影片精簡版逐張 → 小組討論題（可標記已討論）→ 信心宣告 → 本週操練），螢幕保持喚醒、分段計時；並列出討論題、操練與教導誠信檢核入口。
3. **工具** — 321旅程地圖（八大步驟 × 十單元，依閱讀／畫線／宣告／操練／檢核／重讀／完成／分享自動推算）、920操練簿（我的「2」名單、操練紀錄、九樣果子）、關鍵詞軌跡、教導誠信檢核（11 項固定清單，只列出讀過的課）、講員工具箱。
4. **陪讀** — 小智．落實教練：系統提示詞內建 321 理念全文與目前課別的主題經文、操練；每則回答「先聖經、再321、最後一個最小行動」。範例問題十單元各 20 題（正式版，逐題編寫，涵蓋該單元各課與 321 核心概念）。回答可朗讀、收合、收藏、刪除。
5. **我的** — 進度、宣告簿、操練紀錄、畫線筆記（含領受摘要）、收藏、外觀、字級、朗讀聲音、**資料匯出／匯入**（JSON 備份，換手機不遺失）。

## 部署到 GitHub Pages

1. 在 `spch321-sudo` 帳號下新建 repo（例如 `luoshi321`）。
2. 用「Add file → Upload files」把本資料夾全部檔案上傳到 repo 根目錄（不用子資料夾）。單元分包最大的是 u8（3.2 MB），手機上傳請有耐心。
3. Settings → Pages → Source 選 `main` 根目錄。網址會是 `https://spch321-sudo.github.io/luoshi321/`。
4. 之後更新內容，記得遞增 `sw.js` 的 `VERSION`，已安裝的使用者才會看到更新。

## 已知限制／待辦

1. **語音與小智代理沿用領導力 App 的既有請求格式**（azure-tts / xiaozhi-proxy 兩個 Worker），本沙盒無法連線實測，請部署後實機測一輪朗讀與對話。
2. **簡體版為 OpenCC 自動轉換**，未經人工校對；英文版尚未產生（結構已預留 `data.en.*`）。
3. **沒有「想更深」問答**（文稿沒有現成題庫）。可由小組討論題與講解重點半自動生成初稿後審定。
4. **每日提醒**是 PWA 本機通知：只在 App 開著或從背景切回時檢查（iOS 需先「加入主畫面」並允許通知）；沒有伺服器推播。
5. **操練**：161 課來自文稿的「操練一：」標題；12 課由編輯自文稿操練段落整理。

## 版本

v1.0.1 — 新圖示：十字架、金色道路、綠葉與地球的主視覺（去除四角黑邊、修補為全幅純圖），九個尺寸＋1024 主圖。
v1.0.2 — 跟讀捲動的「正在朗讀」句子改為只用顏色底色標示、不再加外框；提詞機頂列新增「A / A+ / A++ / A+++」字級按鈕，四級循環切換並記住設定（state.user.pfont）。

v1.0.0 — 正式版：簡體介面與資料（OpenCC）、繁／简 切換；範例問題十單元 × 20 題正式版；讀經計畫（三種節奏）與每日提醒；複習宣告（第 3／7／30 天）；全套引文經和合本逐字比對定稿（99.96%）；專有名詞採現代通行譯名。


v0.1.0（卷一試用版）— 初版：十單元 173 課全部資料上線；四模式；操練、宣告、920操練簿；旅程地圖、關鍵詞軌跡、教導誠信檢核、講員工具箱；資料備份。

v0.1.1 — 資料更新：全套專有名詞統一為現代通行譯名（約旦、亞細亞、撒馬利亞、大馬士革、阿拉伯、希臘人、凱撒），引文字句依和合本修正 9 處；程式不變。

v0.1.2 — 資料更新：106 處引文依和合本修正（不符 43、微差 63），全套帶出處引文與和合本 99.96% 一致；程式不變。
